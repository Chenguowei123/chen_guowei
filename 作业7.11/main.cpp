#include <iostream>
#include <array>

using namespace std;

int main()
{
    array <int , 10>counts={};


    array <int , 15>bonus;
    for(size_t i=o;i<bonus.size();++i)
        size[i] +=1;


    double number = 0;
    array <double ,12>monthlyTemperatures={};
    for(size_t i=0;i<monthlyTemperatures.size();++i)
    {
        cin>>number;
        monthlyTemperatures[i]=number;
    }


    array<int ,5>bestScores;
    for(size_t i=0;i<bestScores.size();++i)
        cout<<bestScores[i]<<endl;
}
